import 'package:flutter/material.dart';

// const Color quranPagesColor =
// Color(0xffF1EEE5);

// const Color orangeColor=Colors.orange;

// const blueColor=Colors.blueAccent;

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  // اللون الأساسي (أخضر إسلامي)
  static const Color primary = Color(0xFF2C6E49); // أخضر داكن

  // لون ثانوي (ذهبي فاتح)
  static const Color secondary = Color(0xFFF4C95D); // ذهبي / بيج

  // الخلفية الأساسية
  static const Color background = Color(0xFFF9F9F9); // أبيض مائل للرمادي

  // لون العناصر داخل البطاقات
  static const Color cardBackground = Color(0xFFFFFFFF); // أبيض

  // لون النصوص الرئيسية
  static const Color textPrimary = Color(0xFF1B4332); // أخضر غامق جدًا

  // لون النصوص الثانوية
  static const Color textSecondary = Color(0xFF555555); // رمادي متوسط

  // لون الأيقونات أو الأزرار داخل العناصر
  static const Color accent = Color(0xFF90BE6D); // أخضر مائل للزيتي

  // لون للحدود أو الخطوط الخفيفة
  static const Color divider = Color(0xFFE0E0E0); // رمادي فاتح

  // لون للتنبيهات الإيجابية
  static const Color success = Color(0xFF4CAF50); // أخضر زاهي

  // لون للتنبيهات السلبية أو الأخطاء
  static const Color error = Color(0xFFB00020); // أحمر غامق

  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCardBackground = Color(0xFF1E1E1E);
  static const Color darkPrimary = Color(0xFF387D5D);
  static const Color darkTextPrimary = Color(0xFFE1E1E1);
  static const Color darkTextSecondary = Color(0xFFAAAAAA);
  static const Color darkDivider = Color(0xFF2C2C2C);
}
// import 'package:flutter/material.dart';

// class AppColors {
//   static const primary = Color(0xFF2B6777);         // الأزرق الأساسي
//   static const primaryLight = Color(0xFF52AB98);    // الأخضر المائل للأزرق
//   static const secondary = Color(0xFFB2D1D8);       // خلفيات ثانوية
//   static const background = Color(0xFFF8F9FA);      // خلفية التطبيق
//   static const card = Colors.white;                 // كروت
//   static const textDark = Colors.black87;           // نص داكن
//   static const textLight = Colors.white;            // نص فاتح
//   static const dialogBackground = Color(0xFFF2F2F2);// خلفية AlertDialog
//   static const iconColor = Color(0xFF4D9CAB);       // لون الأيقونات
// }
